#include <stdio.h>

int main(){

  // float radius;
  // float pi = 3.1415;
  // printf("Enter radius: ");
  // scanf("%f", &radius);
  // float area = pi * radius * radius;
  // printf("Area of circle is: %f", area);

  int p,q;
  printf("Enter values of p and q: ");
  scanf("%d%d", &p, &q);
  printf("Value of p= %d and value of q= %d", p, q);
  return 0;
}